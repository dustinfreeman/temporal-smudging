# temporal-smudging



### Deploying to GH Pages

Must be done manually:
```
npm run build
npm run deploy
```

It can take a few minutes to update to the public URL.

### Dev

First time: `npm install`
Local dev : `npm run dev`
